// // Centralized Configuration
// // Use your local machine's IP address (e.g., 192.168.x.x) or localhost for Android Emulator (10.0.2.2)
//
// const HOST = 'localhost'; // Change this to your local IP when running on physical device
// const PORT = 3000;
//
// export const CONFIG = {
//   API_BASE_URL: `http://${HOST}:${PORT}`,
//   WS_BASE_URL: `ws://${HOST}:${PORT}`,
// };
